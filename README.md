User Defined Methods:

Count Words= This method counts how many words are there in the string.
Every=This method check every  conditions should be true.If some conditions are true and some are false so it return false .All conditions should be true
Find Duplicate=In this question we can find duplicates value in array
Palindrome=This question is to check whether the given string is palindrome or not.Palindrome checks the given string is same as reverse of the string or not.
At=This function return the element at the given index in the array.
Concat=This method combines the two array.
Include=In this question we can check the include character is present or not in given string.
Indexof=In this method we are going to find the index of the given character in the string and return the index of the character
Join=This function join the elements of an array and return a string.
Pop=This method removes the last element from the array and return the new array without last element.
Push=This method adds a new element at the end of the array and return the new array with added element.
Remove Duplicate=this function removes the duplicate elements from the array and return a new array with unique elements
Replace=This function replace the given value with new value in the string and return the new string.
Reverse=This function reverse the given string and return the new string.
Shift=This method remove the first index element of array
Slice=This function return the elements from the given start index to the end index in the array.
Sort=This method corrects the positioning of array from ascending to descending and descending to ascending.
Split=This function split the given string into an array of words and return the new array.
Trim=this function remove the spaces from the beginning and end of the string and return the new string.
Unshift=This method adds a value at the first index of array
Some=his method check if some conditions are true.If some conditions are true then it return true
ToString=this function return a string representation of the array elements separated by commas.
